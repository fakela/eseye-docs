# Table of contents

## Location

* ```yaml
  type: builtin:openapi
  props:
    models: false
    downloadLink: true
    grouping: by-tag
  dependencies:
    spec:
      ref:
        kind: openapi
        spec: location
  ```

## Margay

* ```yaml
  type: builtin:openapi
  props:
    models: false
    downloadLink: true
    grouping: by-tag
  dependencies:
    spec:
      ref:
        kind: openapi
        spec: margay
  ```

## Package

* ```yaml
  type: builtin:openapi
  props:
    models: false
    downloadLink: true
    grouping: by-tag
  dependencies:
    spec:
      ref:
        kind: openapi
        spec: package
  ```

## Portfolio

* ```yaml
  type: builtin:openapi
  props:
    models: false
    downloadLink: true
    grouping: by-tag
  dependencies:
    spec:
      ref:
        kind: openapi
        spec: portfolio
  ```

## SMS

* ```yaml
  type: builtin:openapi
  props:
    models: false
    downloadLink: true
    grouping: by-tag
  dependencies:
    spec:
      ref:
        kind: openapi
        spec: sms
  ```

## TRIGRILLO

* ```yaml
  type: builtin:openapi
  props:
    models: false
    downloadLink: true
    grouping: by-tag
  dependencies:
    spec:
      ref:
        kind: openapi
        spec: trigrillo
  ```

## Tigrina

* ```yaml
  type: builtin:openapi
  props:
    models: false
    downloadLink: true
    grouping: by-tag
  dependencies:
    spec:
      ref:
        kind: openapi
        spec: tigrina
  ```
